Config = {}

scriptsToRestart = {
    'script_example_one', -- First script to respawn
    'script_example_two', -- Second script to respawn
    -- Add more scripts here
}

restartTimer = 60000  -- Set desired timer in milliseconds (currently wait one minute after server re-/start before restarting scripts listed above)
